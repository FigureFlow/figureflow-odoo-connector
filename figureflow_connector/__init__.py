# AIDEV-NOTE: Data-only module — no business Python.
# This file is intentionally empty so the module is installable on Odoo
# Online (Odoo SaaS only allows third-party modules whose sole Python files
# are __init__.py and __manifest__.py). All behaviour is declarative XML;
# the connect flow runs in the FigureFlow web app. See __manifest__.py.
